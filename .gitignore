/build/
/.vscode/

# freetype subproject
subprojects/*

!subprojects/*.wrap
# can't figure out how to make !subprojects/glm/meson.build to work properly so just run git add -f subprojects/glm/meson.build smil